services:
  quizrush:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
    environment:
      - RUST_LOG=info
      - HOST=0.0.0.0
      - PORT=3000
      - CONFIG_PATH=/app/config
      - MANAGER_PASSWORD=admin
    restart: unless-stopped
